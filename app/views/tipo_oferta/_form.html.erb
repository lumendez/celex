<%= form_for(tipo_ofertum) do |f| %>
  <% if tipo_ofertum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_ofertum.errors.count, "error") %> prohibited this tipo_ofertum from being saved:</h2>

      <ul>
      <% tipo_ofertum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="form-group col-sm-4">
    <%= f.label :nombre, 'Tipo de oferta' %>
    <%= f.text_field :nombre, class: 'form-control' %>
  </div>
</div>

<div class="row">
  <div class="actions col-sm-4">
    <%= f.submit "Enviar", class: "btn btn-md btn-primary" %>
  </div>
</div>
<% end %>
