<%= form_for(cuota_curso) do |f| %>
  <% if cuota_curso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cuota_curso.errors.count, "error") %> prohibited this cuota_curso from being saved:</h2>

      <ul>
      <% cuota_curso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="form-group col-sm-4">
    <%= f.label :nombre, 'Cuota del curso' %>
    <%= f.text_field :nombre, class: 'form-control' %>
  </div>
</div>

<div class="row">
  <div class="form-group col-sm-4">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion, class: 'form-control' %>
  </div>
</div>

<div class="row">
  <div class="actions col-sm-4">
    <%= f.submit "Enviar", class: "btn btn-md btn-primary" %>
  </div>
</div>
<% end %>
