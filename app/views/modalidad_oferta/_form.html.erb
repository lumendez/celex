<%= form_for(modalidad_ofertum) do |f| %>
  <% if modalidad_ofertum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(modalidad_ofertum.errors.count, "error") %> prohibited this modalidad_ofertum from being saved:</h2>

      <ul>
      <% modalidad_ofertum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="form-group col-sm-4">
    <%= f.label :nombre, "Modalidad de oferta" %>
    <%= f.text_field :nombre, class: 'form-control' %>
  </div>
</div>

<div class="row">
  <div class="actions col-sm-4">
    <%= f.submit "Enviar", class: "btn btn-md btn-primary" %>
  </div>
</div>
<% end %>
