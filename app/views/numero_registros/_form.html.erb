<%= form_for(numero_registro) do |f| %>
  <% if numero_registro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(numero_registro.errors.count, "error") %> prohibited this numero_registro from being saved:</h2>

      <ul>
      <% numero_registro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
  <div class="form-group col-sm-4">
    <%= f.label :nombre, 'Número de registro del curso' %>
    <%= f.text_field :nombre, class: 'form-control' %>
  </div>
</div>

<div class="row">
  <div class="actions col-sm-4">
    <%= f.submit "Enviar", class: "btn btn-md btn-primary" %>
  </div>
</div>
<% end %>
